package ExerciciosBee;

import java.util.Scanner;

/* Leia dois valores inteiros. A seguir, calcule o produto entre estes dois valores e atribua esta operação à variável PROD. 
A seguir mostre a variável PROD com mensagem correspondente.   

Entrada
O arquivo de entrada contém 2 valores inteiros.

Saída
Imprima a mensagem "PROD" e a variável PROD conforme exemplo abaixo,
 com um espaço em branco antes e depois da igualdade. Não esqueça de imprimir o fim de linha após o produto, 
 caso contrário seu programa apresentará a mensagem: “Presentation Error”. */


 public class ProdutoSimples {
   /*  public static void main(String[] args) {

        Scanner scanner = new Scanner(System.in);

        // Verificando se os dois números são inteiros
        if (scanner.hasNextInt()) {
            int num1 = scanner.nextInt();
            
            if (scanner.hasNextInt()) {
                int num2 = scanner.nextInt();

                // Calculando o produto
                int prod = num1 * num2; 

                // Imprimindo o produto no formato correto
                System.out.println("PROD = " + prod);
            } else {
                // Caso o segundo número não seja um inteiro
                System.out.println("Presentation Error");
            }
        } else {
            // Caso o primeiro número não seja um inteiro
            System.out.println("Presentation Error");
        }

        scanner.close();
    }
}
    */